# DESIGN.md — AI 新闻辅助工作台

## 项目与用户画像
- 内部编辑辅助工具（非公开网站），用户为广州日报编辑记者 + 部门管理员。
- 使用场景：每天上班打开工作台看日历提醒、线索、评报，属高频办公工具。重信息密度与效率，不做营销感。

## 气质与意象
- 「编辑部案头的值班工作台」：纸质报样的米白底色、朱砂红重点标记、墨黑正文；像一张大编辑桌，三摊内容（日历 / 线索 / 评报）分区清楚，上手零成本。
- 专业、克制、可信，像 Bloomberg Terminal 的信息密度感，但保持中文报纸的温润气质，拒绝互联网营销风。

## Design Tokens
### 色彩
- 背景：暖纸色 `#faf7f2`（不使用纯白大底，减轻长时间阅读疲劳）
- 表面/卡片：`#ffffff`，描边 `#e8e2d8`
- 正文墨色：`#1f1b16`；次要文字 `#6b6257`
- 主强调（朱砂/报纸红）：`#b3392f`，用于关键节点、S 级标记、主按钮
- 辅助：重要度 A 用赭石 `#c87f2d`、B 用墨灰 `#6b6257`
- 状态：成功 `#3f7d5c`、待审 `#b8860b`、停用/错误 `#9a948a`
- 严禁：蓝紫渐变、霓虹科技蓝、大面积高饱和色

### 字体
- 中文：Noto Serif SC（标题，报纸标题感）+ Noto Sans SC（正文/数据/界面）
- 数字与英文：Inter
- 标题用衬线体现新闻庄重感，表格、按钮、标签用黑体保证清晰

### 间距与圆角
- 圆角克制：卡片 8px，按钮/输入框 6px，标签 4px；不使用大圆角胶囊
- 间距以 4px 基准网格，信息密度中等偏高（表格行高紧凑）

### 阴影
- 极轻阴影 `0 1px 2px rgba(31,27,22,0.06)`，靠描边和底色分区，不靠浮层感

## 布局
- 左侧固定导航栏（工作台 Logo + 首页 / 新闻日历 / 新闻线索 / 每日评报 / 系统管理），顶部窄条显示当前用户与角色
- 内容区最大宽度 1400px，卡片栅格
- 表格为主的列表页：筛选条置顶（sticky），数据行紧凑
- 系统管理仅 admin 可见入口
- 首页为「工作内容预览」：未来新闻节点 + 最新线索两块卡片（2 列栅格），每条仅信息密度最高的 2-3 字段（日期+名称 / 媒体+栏目+时间），底部「查看全部」跳转对应模块，管理型统计不占首页
- 新闻日历页：左侧主内容（列表/月历）+ 右侧常驻详情面板（约 32–35%，min-w 320 / max-w 440，sticky 顶部、面板内部超高时滚动；窄屏降级抽屉）。月历不套内滚容器，整月自然撑开页面，页面向下滚动

## 组件规范
- 基于 shadcn/ui，覆盖主题色为朱砂红
- 重要度标签：S=朱砂红底白字、A=赭石、B=墨灰
- 置信度以小进度条/徽章展示，低置信度条目带「待审核」琥珀色标记
- 空状态给出明确引导文案（如"暂无数据，可在后台手动添加"）

### 月历视图（视觉 2 次重构落点）
- 弱化方形网格：极浅细线（`gap-px` + 浅米分隔 `#ece7dc`）分区，不靠粗边框"卡片盒"，格子 hover 暖米底
- 信息层级：公历日期最大（21px 粗体，主视觉）→ 农历小字（11px 浅色）→ 节气/节日小标签 → 事件胶囊（11px）
- 农历/节气/节日仅作辅助展示不落库（`lunar-typescript` 本地计算，见 `src/lib/lunar-calendar.ts`）；节气用紫底小标、节日用赭石浅底小标
- 今天：浅朱砂圆底（`#f5ddd8`）包日期数字 + 朱砂文字，不用粗描边；选中日期格浅朱砂底 `#fdf4f1`
- 非本月补位日期整体降透明度（opacity 45）弱化，其节点标签保留但更浅
- 事件标签沿用分类色系胶囊（浅底+深字+圆点），S 级朱砂加粗文字+徽章；日期格默认 1–3 条，超出「+N 更多」在格子内就地展开
- 月历头部月份标题 `text-2xl` 强存在感；周末星期栏用浅朱砂文字标识

## 交互与状态
- 列表筛选、搜索即时反馈；视图切换（周/14天/月/全部）用 Tabs
- 后台审核操作用 Dialog 二次确认；危险操作（停用/删除）用红色警示按钮
- 流式 AI 输出（评报）打字机渲染，生成中禁止重复提交
- 所有表单错误行内提示，不弹 alert

### 交互状态统一（全系统 UIX 规范）
- 目标：让用户始终知道"操作是否生效、系统正在做什么、进行到哪一步、结果是否更新完成"，不做花哨动画。
- 动画克制：淡入 / 轻微位移 / 进度变化，150–300ms；禁止大幅缩放、旋转、弹跳。
- **布局稳定（防 Layout Shift）**：hover/active/selected 一律**不改变元素外部尺寸**：
  - 恒定 1px「透明边框占位」：交互控件默认 `border border-transparent`，选中/悬停只改`border-color`，禁止「无 border ⇄ 有 border」切换（曾导致 2px 高度位移）。
  - `ui/button` 的 `default/secondary/destructive` 与 `outline` 均恒有 1px border，切换仅颜色/背景变化。
  - 不通过 padding、font-weight、加图标来切换选中（如必须加图标则预留位置）；同组控件固定统一高度（`h-8/h-9`）、统一 gap/padding/border-radius。
  - 阴影仅视觉（hover 阴影、`translate-y-px` 用 transform）不占布局。
  - 全局 `html { scrollbar-gutter: stable; }` 保持滚动条出现/消失时主内容宽度稳定。
- **统一时长 token**（禁止页面内手写其他 duration）：
  - hover：150ms；button active：80–120ms（采用 active:translate-y-px 按压感）
  - 页面切换/内容替换：旧内容淡出 120ms → 新内容淡入 240ms（`PageTransition`，淡入结合 1.5px 轻微上移）
  - toast：淡入 150ms / 淡出 300ms
- **状态统一规则**：
  - hover：主按钮 `变深 + 极轻阴影`；次/ghost/outline `背景变浅 + 边框加深`；link `下划线`；可点击卡片 `浅底(bg-accent/50) + 轻描边(border-ring)`，**一律禁 hover 放大阴影**。
  - active：统一 `translate-y-px` 按压 + 阴影降到最低。
  - focus：统一 focus-visible ring（--ring + 偏移），覆盖导航 Link 与可点击卡片。
  - disabled：opacity-50 + pointer-events-none。
  - selected：左侧导航主色实底；Tabs 浅底+描边；列表项 data 高亮。
- 统一公共组件（`src/components/common/*`，页面禁止各自重复实现）：
  - `LoadingButton`：异步按钮统一loading（spinner + 替换文案 + disabled 防重入），可短暂成功对勾 / 失败叉。
  - `PageSkeleton`：首屏加载骨架，结构与真实内容一致，避免加载完成后跳动。
  - `EmptyState` / `ErrorState`：空状态给明确引导文案（如"今日评报尚未生成"）与可操作按钮；错误可重试。
  - `AsyncArea`：`initialLoading/refreshing/error/empty/hasData` 四态分派；refreshing 时**保留旧结果**并在顶部显示"正在更新"徽标，新结果完成后平滑替换。
  - `TaskProgress` / `Toast`：见下。
- 页面/菜单切换轻量淡入（`PageTransition`，contentKey 变化触发），导航当前项立即高亮，无白屏。
- **AI 进度不伪造**：只有后端/SSE 真实返回的阶段才展示具体步骤（如评报 fetching→analyzing→structure→final→saved）；无真实阶段事件的任务只显示"处理中"，禁止按时间估算进度。
- 长任务不阻塞整页：AI 生成时用户仍可滚动/切换菜单/查历史，禁用全屏 loading 遮罩锁死。
- 结果更新不"闪一下"：重新生成/刷新时保留旧内容 + 顶部"正在更新"提示，完成后替换。
- 操作反馈统一走 `toast`（保存成功/删除成功/停用成功/AI已开始/AI完成/请求失败/网络异常），简短不用确认框。
- 空状态要设计：线索→"今天暂未发现新栏目"、评报→"今日评报尚未生成"、日历→"当前时间范围暂无节点"。
- 后台耗时/危险按钮同样有 loading 反馈与防重入（`disabled` + spinner + 文案变化），危险操作用红色警示按钮。

## 可访问性与文案
- 正文对比度 ≥ AA；重要度不仅靠颜色区分，同时带 S/A/B 文字
- 文案使用编辑部术语：节点、选题、同题、评报、待审、停用
