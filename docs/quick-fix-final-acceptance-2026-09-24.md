# 采集快速修复与逐源验收（2026-09-24）

本轮确认恢复 **84 个 source / 48 家媒体**。这是可直接归因并经过真实回推验证的范围；不把上一轮 source 绑定修复、原有成功源或仅新增配置但抓取失败的源计入。

## 1. 修复结果与统计口径

| 修复项 | 实施范围 | 最终确认通过 | 保留问题 |
| --- | --- | --- | --- |
| 现有 generic 复用 | 83 个 source / 47 家媒体 | 79 个 source / 46 家媒体 | 4 个 source / 4 家媒体仍失败 |
| 相对链接、重定向后的路径与 base href | 内蒙古日报 3 个 source / 1 家媒体 | 3 / 1 | 无 |
| Playwright 详情重解析后保留 URL、source_type、business | 浙江广播电视集团 2 个 source / 1 家媒体 | 2 / 1 | 无 |
| 上述可直接归因范围去重合计 | 88 个 source / 49 家媒体 | **84 个 source / 48 家媒体** | 4 个源 |

另修复了许可证、备案、页脚资质、举报功能页、栏目/版面索引、App 下载、投票/登录/社区论坛子域名的误识别。该规则影响多个来源，与上表重叠，不再次累加 source 数。福建日报链接由 56 增至 153；不将该入口的全部恢复都归因于本轮。

原 134 个缺配置源只注册了有文章正文证据的 83 个，严格用 source_ids 白名单，未启用同媒体未经验证的其他入口。剩余 **51 个 source / 28 家媒体**保留：HTTP 访问未通过 11、没有文章链接 3、详情标题/正文/日期核验未通过 23、缺少足够实质正文段落 14。筛选用的更严格门槛只用于准入验证，生产正文标准没有放宽。

原 43 个列表失败源涉及 16 家媒体：39 个入口返回 HTTP 200，4 个访问未通过；其中 3 个由本轮直接从 0 链接恢复到 109 条，13 个在正确绑定入口后已有链接，23 个仍需后续适配。没有将所有旧列表失败都算成本轮修复。

## 2. 完整 284 源原始验收

Run：`e3f6940c-52f7-44b8-8566-f4fdc2ad4aa0`。队列 **284 个唯一 website source / 135 家媒体**，与开跑前相同。全部 source 均执行到终态；整轮采集结束时，7 个业务代码/配置文件哈希与开跑前一致。两个澎湃新闻源在误抓 APK 后被终止重复尝试，按失败保留，详见下面的补充回归。

| 指标 | 原始完整 run |
| --- | ---: |
| 成功 source | 159 |
| 失败 source | 125 |
| 至少一个源成功的媒体 | 89 |
| 至少一个源失败的媒体 | 70 |
| 同时有成功和失败源的媒体 | 24 |
| 新增文章（后端接收） | 683 |
| 更新文章 | 3 |
| 重复文章 | 794 |
| 后端判定 invalid / 单篇写入 failed | 0 / 0 |
| 人工质量复核隔离 | 4 |
| 扣除已确认误抓后的本轮新增 | **679** |

“源成功”指该 source 至少有有效文章被后端接收或去重成功，不代表其全部候选详情均成功。媒体成功数与失败数有交集，不能直接相加。后端 invalid=0 不等于内容全部合格，4 条人工发现的非正式新闻已另行隔离。

282 个有结束报告的 source，五项身份字段及 ingest.perSource 均一致；2 个 APK 超时源无结束报告，不能算成已通过身份回传验证，但调度输入和 source_start 日志保留其真实 ID。后续两个源均在定点复验中通过身份核对。

### 原始失败分类（按 source 去重）

| 日志可见的主要失败阶段/原因 | source 数 | 涉及媒体数 |
| --- | ---: | ---: |
| 网络/TLS/超时 | 4 | 4 |
| 未提取到列表链接（未证明当天无更新） | 40 | 20 |
| HTTP 5xx（站点或代理） | 14 | 14 |
| WAF/403/412/访问拒绝 | 9 | 5 |
| 配置缺失（未通过通用复用验证） | 51 | 28 |
| 后端入库校验/接收失败 | 1 | 1 |
| 非新闻 App 下载误识别（超时） | 2 | 1 |
| 详情无有效文章 | 3 | 2 |
| URL/入口不可用 | 1 | 1 |

各类媒体有交集，不能按类别累加。空列表不等于“当天无更新”；HTTP 5xx 无法仅凭代理出口日志区分站点还是中间代理，未做武断归因。

## 3. 完整运行后发现的问题与定点收尾

澎湃新闻候选列表第 10 条是“Android版”，`https://m.thepaper.cn/download?id=2` 会跳转至 APK，响应类型 `application/octet-stream`、长度 **101230806 字节**。前期 3 条小样本没有覆盖到这一项。确认后终止错误下载及重复尝试，保留原始失败，不把终止伪造为正常完成。

补充回归 run：`8461282f-8bf9-4010-8530-f2345fdc2726`，上限 10 篇/源。

| source / 媒体 | 接收文章 | 结果 |
| --- | ---: | --- |
| 1954afb6… / 澎湃新闻 | 9 | PASS，首次尝试，无 APK |
| 5b231dec… / 澎湃新闻 | 9 | PASS，首次尝试，无 APK |
| 22c34aa8… / 青岛日报 | 10 | 抓取回推通过；随后追加功能/社区子域名排除 |
| 1c89bfc7… / 湖南日报 | 10 | PASS，举报页已排除 |

青岛最终质量回归 run：`77b269c5-9b38-4b61-97ae-68373562b070`。10 条候选中 9 篇新闻回推成功，1 条直播详情 15 秒超时被跳过；最终结果不含举报、服务系统、论坛和下载页。全部定点回归 source_id / media_id / source_url / source_type / crawl_method 与回推一致。

完整 run 后两次补充回归合计新增 **12 篇**、重复 **35 篇**，invalid / 写入失败均为 0；这些数量没有混进原始 284 源 run。原始 run 与补充回归合计净新增 **691 篇**（683 + 12 - 4）。早于该完整 run 的筛选/小样本/中断试跑不包含在此数字中。

综合原始完整运行与定点复验，当前最新已验证覆盖为 **161 个 source / 90 家媒体**，另有 123 个 source 未通过；24 家媒体同时存在成功和失败入口。这是“两阶段最新结果”，不是第二次完整 284 源运行。

4 条隔离记录仅设置 is_test=true，保留原文证据，没有删除历史数据：青岛论坛举报页、湖南举报功能页、青岛网络支援服务系统页、青岛社区论坛帖。服务系统页曾被处理，但查询到的 news_clue_article 关联为空；论坛帖关联也为空。

## 4. 已补配置但仍保留的 4 个源

| 媒体 | source_id | 真实结果 |
| --- | --- | --- |
| 山西日报 | a7eecada-fb58-4d73-8864-d70792dc46a4 | 抓得 10 篇正文，后端返回“数据源不存在”；生产库只读核查该记录存在且启用。ingest 将源查询失败与不存在合并处理，原始查询错误未保留，不能断言具体网络原因。 |
| 长江日报 | 34357650-ec67-4c9a-8b01-f6a4f338c30b | HTTP 入口返回 502 |
| 法治日报 | d9ec8081-73c2-4460-a374-93fba15760ed | 90 条候选，抽取 10 条详情均未通过标题/正文校验 |
| 锡林郭勒盟日报 | 9107eb61-1648-4e8b-b249-6353e881cc06 | HTTP 入口返回 502 |

本轮未绕过 WAF/403/412，未做复杂 SPA/API 逆向，未关闭 TLS 校验，也未凭猜测更换未确认的入口。持续超时、站点/代理 5xx、缺少可靠通用解析证据的源保留为后续适配范围。单独 epaper 小样本正文已恢复，但人民日报数字报模板中的旧发布日期问题仍保留，不混入本次 website 284 源验收。

## 5. 自动线索阶段的真实结果

主调度在 16:39:59 记录 `fetch failed`，因此原始 media_then_clues run 状态仍为 failed，没有改写。

服务端继续执行，16:47:59 的生产 task_log 记录：扫描 **674 篇**，处理 **673 篇**，发现线索 **10 条**，1 篇 AI JSON 解析/格式校验失败。随后只重试该 1 篇，run `99def25a-0a78-4d45-93ba-7c04450e6bb3` 返回 HTTP 200、success=true、处理 1、错误 0、pending=0。没有为此重跑媒体队列或改 AI 代码。

因此识别待处理队列已清空，但不能宣称原始自动调度全链路无错误：长请求等待返回失败仍需后续修复；服务端还观察到 `news_clue_series_key_idx` 的 23505 唯一键冲突日志（人民日报-一见），本轮未扩改线索存储逻辑。处理标记和发现线索数量不等于已经证明所有线索关联均完整。

674 是近 3 天、监测媒体、非测试且待处理文章的业务筛选结果，包含此前待处理数据，不应与本轮新增 683 直接做一一对应。

## 6. 验证与交付文件

- 10 项 Python 自动测试通过；最终规则没有拦掉此前用于 generic 准入验证的真实文章样本。
- Python 编译与 git diff --check 通过；未改 UI，未 commit/push。
- 原始完整记录：[JSON](quick-fix-final-acceptance-2026-09-24.json)、[逐源 CSV](quick-fix-final-sources-2026-09-24.csv)、[逐媒体 CSV](quick-fix-final-media-2026-09-24.csv)。
- 最新两阶段状态：[逐源 CSV](quick-fix-latest-source-status-2026-09-24.csv)、[逐媒体 CSV](quick-fix-latest-media-status-2026-09-24.csv)。
- 筛选证据：[134 个缺配置源](quick-fix-config-sources-2026-09-24.csv)、[保留的 51 个源](quick-fix-retained-configs-2026-09-24.csv)、[43 个列表问题源](quick-fix-list-screen-2026-09-24.csv)。
- 补充验证：[四源回归](quick-fix-supplemental-regression-2026-09-24.json)、[青岛最终质量回归](quick-fix-quality-final-2026-09-24.json)、[线索阶段观测](quick-fix-clue-stage-observation-2026-09-24.json)、[1 篇重试](quick-fix-clue-retry-2026-09-24.json)。

交付口径：**本轮直接确认恢复 84 个源 / 48 家媒体；当前已验证可用 161 个源 / 90 家媒体。采集修复与逐源验收完成，已知限制有据可查；原始自动调度的长请求及线索唯一键问题单独保留，不能承诺全链路无错误。**
