# 2026-09-23 真实数据交付验收

核查日期：2026-09-24；直接查询当前生产 Supabase 的 article 表，并调用当前服务的真实候选查询与选稿 API。未写入模拟文章、未改文章日期、未降低规则门槛。

## 结论：FAIL，阻塞在文章采集入库

统计口径：正式文章 is_test=false，发布时间为北京时间 2026-09-23 00:00（含）至 2026-09-24 00:00（不含）。同时独立核查 created_at，排除“已入库但发布时间落在其他日期”的歧义。

| 阶段 | 9 月 23 日实际结果 |
|---|---:|
| 当日发布的正式文章 | 0 篇 |
| 文章来源媒体 | 0 家 |
| 当日 created_at 新入库正式文章 | 0 篇 |
| 当前默认评报媒体范围命中 | 0 篇 |
| 2000 字过滤后 | 0 篇 |
| 按媒体、标题去重后最终候选 | 0 篇 |
| 是否进入 AI 选稿 | 否 |
| 聚类 / 新华社转载判断 / 同行独有 | 未执行，无输入文章 |
| 生成评报 | 未执行 |
| 新闻线索按该日样本识别 | 无输入文章，未执行 |

当前默认媒体为广州日报、南方日报、南方都市报、新快报、羊城晚报、信息时报，兼容广州日报报业集团，实际匹配 7 个媒体 ID。阈值为后台当前生效的 2000 字，没有因样本不足放宽。

真实 POST /api/review/draft，date=2026-09-23，返回 HTTP 400；diagnostics: total=0、mediaCount=0、selected=0、afterWords=0、eligible=0。不能用这个样本证明后续 AI 业务链路通过，也不能将未执行的阶段说成 AI 故障。

## 最近真实数据是 9 月 22 日

不是 9 月 23 日。生产库最新正式文章 publish_time=2026-09-22T10:00:00Z，即北京时间 18:00。

只读候选验收：151 篇 / 18 家媒体 → 默认评报媒体 62 篇 → 字数过滤后 22 篇 → 标题去重后 21 篇。

9 月 22 日不是本次指定的完整链路验收样本，因此仅验证查询数量与日期切换，没有自动生成该日新评报或执行该日 AI 线索识别。

## 9 月 24 日媒体抓取最终根因

- 真实运行 ID：b22ad385-4560-41b0-9e6a-96be20b67fc9。
- Windows 计划任务已运行，开始 09:19:07，结束 09:48:09（北京时间）。不是“没有触发”。
- 284 个数据源步骤均退出失败，0 篇文章，未触发 clue_identify。284 是源数，不一定是媒体家数。
- 父任务请求当前主服务 3002 成功，拿到队列并启动 Python 子进程；子进程却读取 sidecar 旧配置 MAIN_API_BASE=http://localhost:3001，连接旧端口失败。
- 原始 media-fetch.log 的本次运行时间段反复出现 `[ingest] ... All connection attempts failed`，随后 `RuntimeError: No matching website source in ingest queue`。这是获取抓取队列前的连接失败，不能解释成 284 家网站都没有新闻。
- 当前健康接口 HTTP 200，database=ok、ingest_enabled=true；Python 子进程确实启动。该失败发生在 NO_PROXY 直连的本地主服务端口，不是外网媒体解析或代理预热阶段。
- 旧 journal 的 phase 一直停在 task_switch，是阶段记录缺漏。本次修复为网络准备、队列读取、媒体抓取分别记录真实阶段。
- 地址继承已修复：子进程显式接收父任务 MAIN_API_BASE 和 INGEST_API_TOKEN；真实 IngestClient 只读验证已取回 322 个源的完整队列，其中 website 为本轮 284 个。
- 没有重写失败批次为成功，也没有重跑整个抓取批次；今天文章仍为 0。连接修复通过不等于文章采集、AI 识别及评报全链路已经通过。

原始证据（位于本机，未纳入 Git）：

- logs/scheduler/b22ad385-4560-41b0-9e6a-96be20b67fc9.json
- logs/scheduler/media-fetch.log
- logs/scheduler/media_then_clues-launcher.log

## 页面与 API 最小修复

- 新增只读 GET /api/review/availability?date=YYYY-MM-DD；统计严格复用真实候选查询，分页统计媒体数，失败不冒充零篇。
- 日期下展示当日文章/媒体、各层筛选数量、最近有数据日期；“使用最近有数据日期”只切换日期，不自动触发 AI。已实际点击验证切到 09-22，并显示 151/18、62、22、21。
- 选稿错误附真实阶段，AI 是否进入由实际调用位置标记；选稿期间防重复提交。
- 线索摘要基于同一运行时间段的日志解释失败阶段，过滤日志 ANSI 颜色影响，显示“抓取队列读取失败 → 未进入媒体采集 → 未进入 AI 识别”。
- 不再把另一轮 AI 识别日志混入当前抓取批次；本批次未进入 AI，处理文章、批次和调用均为 0。
- 保留原 UI 风格。本轮未提交、未推送。

验证：日期与阶段诊断 8 项测试通过；TypeScript 通过；新增与诊断相关文件 ESLint 通过；原 review-engine AI 解析部分仍有既存 explicit-any 问题，不属于本次更改。
