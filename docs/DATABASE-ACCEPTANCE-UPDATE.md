# A/B执行与持久化验收更新（2026-09-17）

本报告覆盖ACCEPTANCE-REPORT.md中的数据库凭据/缺结构阻断结论。数据库认证及TLS校验已通过，连接确认是扣子共享生产项目reusdpelytqggjyhfsyh；未关闭证书校验。

## 数据库实际结果

| 项目 | 实际结果 |
|---|---|
| A v2结构兼容 | PASS，已提交 |
| 历史去重迁入 | 新增108条calendar_event，121条历史来源关联 |
| 歧义暂缓 | 10条，不强行合并 |
| 历史资料 | 未物理删除，前台仅读calendar_event |
| 当前30天正式节点 | 12条，`/api/calendar?view=month` HTTP200 |
| 首页未来7天 | 3条具体节点，无日历告警 |
| B类结构 | recent_article_at、news_clue_article、review_draft、daily_review_revision已补齐 |

A run_id：`500a786b-faba-4b19-9223-e446c77983f6`。前后快照、写入指纹、来源映射及暂缓记录在`logs/calendar-import/该run_id/manifest.json`及同目录JSON；108条中新建93条日期明确、13条仅月份、2条未知日期。原正式表3条保留，仅补充已知来源标签；不恢复停用或删除节点。

B run_id：`0dade9fe-d376-4fe7-9f5c-86749cf8323b`。记录在`logs/business-schema/该run_id/manifest.json`，执行前已有线索12条、评报1条。B仅补结构，没有改写原报告或线索；三张新表初始均为空、启用RLS并限制为服务端访问。

回滚定位/指纹保护方案见db-review/calendar-a-v2-plan.md。没有执行回滚或删表。B完成后新表已产生验收数据，不能直接DROP当作无损回滚；需要数据快照和单独审批删除/不可逆动作。

## 实际HTTP验收

| 验收项 | 结果 |
|---|---|
| 日历200、当前30天12条、详情 | PASS |
| 隔离临时节点创建/编辑/停用/软删除后隐藏 | PASS |
| 首页节点预览与具体新栏目 | PASS |
| Mock ingest、去重、补全更新 | PASS |
| DeepSeek实际调用 | PASS |
| 本期选稿实际生成与随后读取 | PASS |
| 评报完整生成并保存 | PASS，生成918字、saved事件 |
| 补充要求后完整重生成并保存 | PASS，四模块932字，v1→v2 |
| 隔离新栏目识别持久化 | PASS，2个新栏目与原文URL关联 |
| 旧栏目排除、重复识别不新增 | PASS |
| 确认新栏目 / 不是新栏目持久化 | PASS |
| 手动clue_identify任务 | PASS，任务最近执行/下次执行/状态可读取 |

实际证据：

- 全量写入验收：`logs/acceptance/e3ff0d73-815d-46db-bd28-30d56bc088a0/results.json`，26 PASS/1 FAIL。唯一FAIL是在首次选稿生成前GET返回404；该次后续生成已成功，不是缺表。验收脚本已区分首次404和持久化读取，避免误判。
- 选稿存在后的HTTP复查：`logs/acceptance/31212c3e-9604-4fd3-95d9-36efd91d4de2/results.json`，19/19 PASS。
- 线索隔离HTTP专项：`logs/acceptance/clues-c401a700-98a4-43d6-9d5d-7879fff9103d.json`，7/7 PASS。文章只通过article-v1 ingest推送，没有直接写数据库。
- 评报SSE专项：`logs/acceptance/review-http.json`，run_id `5fa644be-661d-4e7e-9fc9-b8c17c22568c`，3/3 PASS。只对本次新生成的评报重生成，未修改9月12日既有评报。

验收保留带run_id的Mock文章、新栏目和本次评报/版本，例子链接example.test不是可打开的真实报道。日历临时节点采用软删除，不物理清理。不能将Mock数据当作三家真实采集成果。

## 尚未能宣称全量产品验收通过的项

- 首页仍无每日评报预览区域；此前FAIL仍有效，本轮按不新增功能要求未新增模块。
- 浏览器已验证有数据月历：未来30天12节点、9月具体日期卡片可见；点击横琴节点已打开详情，历史来源/背景/编辑停用删除按钮正常展示。线索页显示已确认隔离新栏目和关联原文URL；对本轮Mock「湾区社区观察」点击确认后，状态变已确认、按钮消失、待确认数归零。其余交互仍需补充复验；HTTP持久化PASS不等同全部浏览器交互PASS。
- 真实媒体原文可打开、真实同题/同行独有/新华社排除需真实数据PoC再复核；现有AI引擎内存专项5/5仍属于辅助证据。
- 10条歧义记录暂缓；候选生成/粘贴/历史确认旧写入路径仍需核对，不能因前台取消审核门槛就声称所有来源已直写正式表。
- 外部自动定时尚未验证，手动任务PASS不等于自动调度已上线。

三家媒体PoC尚未开展。本轮未重构业务、未新增UI功能、未更改独立抓取服务；先完成余下现有功能操作验收，再严格按广州日报→南方日报→南方都市报验证真实抓取和HTTP联动。
