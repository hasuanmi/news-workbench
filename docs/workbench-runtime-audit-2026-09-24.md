# 工作台运行逻辑核查

核查窗口：2026-09-23 至 09-24。下列时间均为北京时间；数据为核查时快照。

## AI 日历推荐

- 自动执行依赖 Windows 计划任务，不是打开网页就触发。`GZDaily-CalendarRecommend` 已安装且启用，配置每天 07:30。
- `task_log` 最近成功业务执行：09-18 09:17，新增 0 条；09-17 23:15、23:17 分别新增 8、2 条。
- `calendar_event` 实存 10 条已启用 AI 来源记录：`source=ai_recommend`、`source_type=ai_supplement`，页面统一显示“AI推荐”。这些是旧版推荐的固定节点，不能据此宣称新版动态推荐成功。
- 日期范围为 10-24 至 12-10。09-23 的未来 30 天截至 10-23，因此该筛选下没有 AI 节点；全年接口包含全部 10 条，非写入丢失。
- 最近系统触发 09-22 09:25，退出码 3221225786，未正常完成。最近有完整 worker 日志的失败发生在 09-20 09:40，停在 main_health；main-server.log 有 3001 端口占用。核查时 3001 不通、3002 健康。
- 本地忽略文件 `.env.local` 的 `LOCAL_MAIN_API_BASE` 已指向 3002；worker 启动服务时同步使用目标地址的端口。健康接口和带鉴权的任务开关接口验证成功。
- 跨日后新增真实成功记录：09-24 09:19:11 至 09:19:15，Windows 计划任务自动触发，数据库 task_log 同步成功。取证 2 页，模型候选 0、新增 0；推荐窗口为 09-24 至 12-31。没有手动触发 AI 生成来冒充自动运行。

## 每日评报

- 原默认日期来自当前时间；日期选择器选中本地午夜后 `.toISOString()` 再由后端截 UTC 日期，会错查前一天。
- 默认媒体配置未覆盖，使用广州日报、南方日报、南方都市报、新快报、羊城晚报、信息时报，兼容广州日报报业集团，共 7 个数据库媒体 ID。
- 默认字数门槛 2000；正式数据必须 `is_test=false`，`publish_time` 落在北京时间当日，按媒体范围和字数筛选，再按媒体与标题去重。
- 09-23：全部媒体正式文章 0 篇、默认媒体 0 篇、候选 0 篇。该日确实没有已入库文章，并非单纯阈值过高。
- 09-24 再次核查，正式文章仍为 0。
- 09-22：全媒体 151 篇、默认媒体 62 篇、字数达标 22 篇，标题去重后实际候选 **21 篇**。
- 已改为日期字符串传输、北京时间解释时间戳、次日零点开区间，分页读取候选；空结果返回真实计数及明确原因。数据库错误仍报错，不伪装成无文章。
- 原 09-23 选稿请求复核 HTTP 400，diagnostics 为 total=0 / selected=0 / eligible=0。未自动放宽阈值、未改成昨天、未调用 AI 生成评报。

## 首页与日历浏览

- 首页待确认线索 18 条；本年信息待补全节点 4 条，计数复用正式日历质量规则和去重口径。
- 09-22 16:04 抓取批次为 51/284 个源成功；09-24 09:19 新批次为 0/284，首页随真实执行记录更新为失败。
- 09-24 全失败直接原因：Python 子进程依旧读取 sidecar 的 MAIN_API_BASE=http://localhost:3001，日志显示队列连接失败，尚未进入真实文章抓取。已在 worker 的子进程环境显式传入 MAIN_API_BASE 和 INGEST_API_TOKEN，统一父子任务目标。真实 Python IngestClient 只读验证成功，拿到 322 个源的完整队列（其中包含广州日报）；未重跑全部 284 个网站，后续抓取和入库成功数仍需真实执行确认。
- 首页 AI 状态综合数据库、worker 日志及系统触发结果；较新的启动失败不会被旧成功记录覆盖。
- 当前年全年列表保留所有节点，通过可滚动时间轴初始定位今天；即使今天没有节点也有锚点。向上保留过往节点，历史/下一年从年初浏览；月历仍支持自由翻月。

## 验证

- 25 项日历和日期纯函数回归测试通过；TypeScript 检查通过。
- 已核实全年 API 的 102 条日期明确节点和 10 条 AI 来源节点，未来 30 天筛选的日期边界，以及真实评报只读候选查询。
- ESLint 检查发现 review-engine.ts 原有 AI 解析部分 12 处 explicit-any 错误，位于本次未修改的逻辑；本次新增代码未引入 any。
- 本轮未提交或推送。
